import { Stack } from "@chakra-ui/react";
import { ConfigureRoyalty } from "../ConfigureRoyalty";

export const ConfigureBundle: React.FC = () => {
  return (
    <Stack spacing={4}>
      <ConfigureRoyalty />
    </Stack>
  );
};
