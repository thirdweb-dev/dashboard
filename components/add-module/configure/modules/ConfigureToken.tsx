import { Stack } from "@chakra-ui/react";
import { ConfigureSymbol } from "../ConfigureSymbol";

export const ConfigureToken: React.FC = () => {
  return (
    <Stack spacing={4}>
      <ConfigureSymbol />
    </Stack>
  );
};
