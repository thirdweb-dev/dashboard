import { Stack } from "@chakra-ui/react";
import { ConfigureRoyalty } from "../ConfigureRoyalty";

export const ConfigurePack: React.FC = () => {
  return (
    <Stack spacing={4}>
      <ConfigureRoyalty />
    </Stack>
  );
};
